#pragma once

#include <stdbool.h>
#include <stddef.h>

// Формирует короткий идентификатор комнаты на основе MAC-адреса устройства.
// В out записываются ровно 6 заглавных HEX-символов без разделителей (младшие 3 байта MAC).
// Возвращает true при успешном получении MAC и форматировании. В случае неудачи возвращает false,
// содержимое out не определено (вызывающий код должен подставить запасное значение).
bool room_id_format(char *out, size_t out_len);
